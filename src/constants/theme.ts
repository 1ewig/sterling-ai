/**
 * Canonical brand color hexes for browser meta, manifest, and system chrome.
 * (JSX components and UI styles must use semantic CSS tokens from globals.css).
 */
export const BRAND_THEME = {
  BG_HEX: '#09090b',
  PRIMARY_HEX: '#00f0ff',
} as const;
